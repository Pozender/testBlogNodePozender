<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <title>Blog</title>
</head>
<body>
   <div class="container">
    <h1 class="mt-3 mb-4">Articles de blog</h1>
    <a class="btn btn-outline-success" href="/articles/new">Nouveau Article</a>
    <% if(articles != "") { %>
        <% articles.forEach(article =>{ %>
            <a href="/articles/<%=article.id%>" class="link-dark text-decoration-none">
                <div class="card mt-4">
                    <div class="card-body">
                        <h2 class="card-title "><%= article.title %></h2>
                        <div class="card-subtitle mb-2 text-muted"><%= article.date.toLocaleDateString() %></div>
                        <p class="card-text"><%= article.description %></p>
                    </div>
                    
                </div>
            </a>
         <% }) %>
    <% } %>
    
    
   </div>
</body>
</html>